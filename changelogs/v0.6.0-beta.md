## v0.6.0-beta - 2026-05-29

데이터 소스 전면 장애 대응 — Yahoo 프록시 자동 갱신 + Stooq fallback + FMP/캐시 보강, 그리고 harness 툴링 도입. 27 files changed, +1429 / -73 lines.

### Added

- **Webshare API 기반 Yahoo 프록시 풀 자동 갱신** — PR #52
  - `WebshareProxyClient` + 스케줄러(시작 시 1회 + 1시간마다)로 프록시 풀 hot-swap
  - 기존 `YAHOO_PROXY_URL` 수동 주입 제거, Webshare 일원화
- **Stooq fallback 소스 추가** — PR #52
  - 지수/선물/상품/환율 10종 보강 (TwelveData → Stooq → Yahoo → Finnhub)
  - Yahoo 차단 시에도 대시보드 주요 지표(S&P500/Nasdaq/Dow/상품/환율) 표시

### Changed

- **harness 가드레일/툴링 도입** — PR #52
  - `harness.config.json`, `.claude/rules`·`hooks`, husky 훅(pre-commit 병합 유지)
  - FE 스캐폴더 타입 매핑 (component/util/model → apps/web/src)
- **`/release` 명령 추가** — changelog + Release PR + root package.json version 동기화를 한 번에
- **Yahoo chart 캐시 TTL 2분 → 3분** — crumb/429 호출 빈도 완화

### Fixed

- **FMP sector-performance 404 수정** — `/sector-performance-snapshot` 전환 (date 필수 + 거래소별 집계) — PR #52
- **pnpm 11 빌드 스크립트 승인** — `allowBuilds`(sharp/unrs-resolver), `make install` 실패 해결 — PR #52

### Statistics

| Item | Count |
|------|-------|
| Files changed | 27 |
| Lines added | +1429 |
| Lines deleted | -73 |
| Merged PRs | #52 |
