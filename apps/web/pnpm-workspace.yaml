# pnpm 11+ 빌드 스크립트 허용 설정 (strictDepBuilds 기본 true).
# sharp: Next.js 이미지 최적화 native 모듈 / unrs-resolver: ESLint import resolver native 바이너리.
# 둘 다 신뢰 가능한 표준 패키지라 빌드를 허용한다 (미허용 시 next build / eslint 동작 불가).
allowBuilds:
  sharp: true
  unrs-resolver: true
